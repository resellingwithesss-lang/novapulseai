node_modules
dist
.env
.env.*
!.env.example
*.log
coverage
.git
.gitignore
*.md
tmp
clips
generated
.vscode
.idea
# NOTE: `cookies.txt` is intentionally NOT excluded here right now. It is
# baked into the image at /data/yt-cookies.txt as a temporary Railway unblock
# (see the cookies comment block in `server/Dockerfile`). Restore the
# `cookies.txt` exclusion here once the deploy switches to a Railway Volume
# or Secret File mount.
