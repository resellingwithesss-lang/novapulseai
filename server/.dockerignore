node_modules
dist
.env
.env.*
!.env.example
*.log
coverage
.git
.gitignore
*.md
tmp
clips
generated
.vscode
.idea

# Local operator secrets — never enter the Docker build context. Cookies are
# provided at runtime via a Railway Volume / Secret File mount (see
# `server/Dockerfile` + `docs/YOUTUBE_CLIPPER_OPERATORS.md`).
cookies.txt
*.cookies
